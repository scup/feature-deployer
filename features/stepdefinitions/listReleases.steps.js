const { Then } = require('cucumber')

Then('It list tags on that environment of current project', function (callback) {
  callback(null, 'pending')
})

Then('It list tags on that environment on scup-care-front and scup-care', function (callback) {
  callback(null, 'pending')
})
