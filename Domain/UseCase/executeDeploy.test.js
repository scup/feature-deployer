describe('executeDeploy', function () {
  context('when production deployDescription should be passed', function () {
    it('throw an expection telling that deployDescription is necessary', function () {

    })
  })
})
